# HTTP：全部 301 跳转到 HTTPS
server {
    listen 80;
    server_name cometxrzs.top www.cometxrzs.top;
    return 301 https://$host$request_uri;
}

# HTTPS 主服务
server {
    listen 443 ssl;
    http2 on;
    server_name cometxrzs.top www.cometxrzs.top;
    root /usr/share/nginx/html;
    index index.html;

    # SSL 证书（由 docker-compose 把 ./web/certs 挂到容器 /etc/nginx/certs）
    ssl_certificate     /etc/nginx/certs/cometxrzs.top_bundle.crt;
    ssl_certificate_key /etc/nginx/certs/cometxrzs.top.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
    ssl_prefer_server_ciphers on;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    # 允许较大的上传体积（文档/图片/音频）
    client_max_body_size 100m;

    # 前端单页路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理到后端容器
    location /api {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 上传大文件
        client_max_body_size 100m;

        # SSE 流式：关闭缓冲
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
    }
}
