你是一位严谨的研究信息提炼专家。下面是为研究主题检索到的**一篇资料**，请从中提炼出**与主题相关的关键要点**。

{% if today %}今天日期：{{ today }}（涉及时效时以此判断）{% endif %}

## 研究主题
{{ topic }}

## 研究提纲（帮助你判断哪些信息有用）
{% for h in headings %}- {{ h }}
{% endfor %}

## 这篇资料（来源编号 {{ source_index }}）
标题：{{ source_title }}
内容：
{{ source_content }}

## 你的任务
逐条提炼这篇资料里**与研究主题/提纲相关**的要点，每条是一个独立的事实/数据/观点/结论。规则：
- **只提炼这篇资料里真实存在的信息，不补充外部知识、不编造。**
- 每条要点写成完整、可独立理解的一句话（含具体对象、数据、结论），不要泛泛而谈。
- **过滤噪声**：广告、导航、与主题无关的内容一律不要。
- 给每条标 relevance（0~1，与研究主题的相关程度），低相关的也可提炼但给低分。
- 如果该信息带**时间/截止日期/有效期**，填到 date_hint（如 "2026-03" 或 "2025.09.30截止"），无则空字符串。
- 若整篇资料与主题基本无关，返回空数组 `{"learnings": []}`。

## 输出格式（严格 JSON，只输出 JSON，不要解释、不要 markdown 代码块）
{
  "learnings": [
    {"text": "要点内容", "relevance": 0.8, "date_hint": ""}
  ]
}
